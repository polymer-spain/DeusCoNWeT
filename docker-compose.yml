version: '3'

services:
  picbitTest:
    image: 'deus/picbit:mongo'
    ports: 
      #- 80:80
      - 8080:443
    volumes: 
      - /home/miguel/DeusCoNWeT:/var/www
      - /home/miguel/logs/picbitMongo:/logs
    restart: always
    command: ['bash', '/var/www/server_config/init.sh']
  picbit:
    image: 'deus/picbit:mongo'
    ports: 
      #- 80:80
      - 8000:443
    volumes: 
      - /home/miguel/logs/picbitMongo:/logs
    restart: always
    command: ['bash', '/var/www/server_config/init.sh']